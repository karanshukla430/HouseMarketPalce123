name: Automated Pull Request

on:
  push:
    branches:
      - version_update_2

# jobs:
#   create-pull-request:
#     runs-on: windows-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3
    
#     - name: Create pull request
#       uses: karanshukla430/HouseMarketPalce123@v5
#       with:
#         token: ${{ secrets.GITHUB_TOKEN }}
#         commit-message: Automated Pull Request
#         title: Automated Pull Request
#         body: This pull request was automatically generated.
#         branch: master
#         base: version_update_2

jobs:
  createPullRequest:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Make changes to pull request
        run: date +%s > report.txt

      - name: Create Pull Request
        id: cpr
        uses: karanshukla430/HouseMarketPalce123@v5
        with:
          token: ${{ secrets.PAT }}
          commit-message: Update report
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          signoff: false
          branch: version_update_2
          delete-branch: true
          title: '[Example] Update report'
          body: |
            Update report
            - Updated with *today's* date
            - Auto-generated by [create-pull-request][1]

            [1]: https://github.com/karanshukla430/HouseMarketPalce123
          labels: |
            report
            automated pr
          assignees: peter-evans
          reviewers: peter-evans
          team-reviewers: |
            developers
            qa-team
          milestone: 1
          draft: false